import styled from 'styled-components';
import { motion } from 'framer-motion';

export const CvValue = styled(motion.p)`
	margin: 1em;
	color: white;
	cursor: pointer;
	border: solid 1px;
	border-radius: 5px;
	padding: 0.4em;
	width: 65%;
`;
