import styled from 'styled-components';

export const GlassContainer = styled.div`
	display: flex;
	align-items: center;
	align-self: center;
	justify-content: center;
	border-radius: 16px;
	overflow: hidden;

	content: '';
	top: 0;
	left: 0;
	right: 0;
	bottom: 0;
`;
